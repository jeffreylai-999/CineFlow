# Triage labels

The canonical roles map directly to these GitHub labels:

- `needs-triage`: maintainer evaluation required
- `needs-info`: waiting for reporter information
- `ready-for-agent`: fully specified and ready for an agent
- `ready-for-human`: requires human implementation
- `wontfix`: will not be actioned

When a skill mentions a canonical role, use the corresponding label above.
